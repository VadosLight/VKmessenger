package com.example.vadim.vkmessenger;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class Dialog_Screen extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_dialog__screen);

        //C#//var ioi = api.Messages.GetHistory(id, false, out value);


        //final VKRequest request = VKApi.messages().get(VKParameters.from(VKApiConst.MESSAGE, ))
    }
}
