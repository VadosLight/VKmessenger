<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#ffffff</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/vk_share_gray_line</item>
    </style>

    <style name="Button" parent="@style/Base.TextAppearance.AppCompat.Button">
        <!--Добавить стиль для всех кнопок -->
    </style>

<style name="ListViewColor" parent="Base.Widget.AppCompat.ListView">
    <item name="android:background">#000000</item>

</style>
</resources>
